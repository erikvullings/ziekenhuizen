import m from 'mithril';
import { MeiosisComponent } from '../services/meiosis';

export const HospitalCostModule: MeiosisComponent = () => {
  return {
    view: () => {
      return m('div', 'Todo ziekenhuiskosten');
    },
  };
};
